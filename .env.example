# Client-exposed config (safe to commit, but real values still live in .env / Vercel).
# Server-only secrets (Stripe, WordPress, Supabase service role) are NEVER listed
# here and NEVER use the VITE_ prefix — set those in Vercel → Settings →
# Environment Variables.

VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=
VITE_WP_GRAPHQL_URL=
VITE_PAYMENTS_CLIENT_TOKEN=